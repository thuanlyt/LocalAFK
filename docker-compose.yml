services:
  localafk:
    build: .
    container_name: localafk
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - HOST=0.0.0.0
    env_file:
      - .env
    volumes:
      - ./data:/app/data
